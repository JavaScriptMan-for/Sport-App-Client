import { FC } from "react";
import { SafeAreaView } from "react-native-safe-area-context";
import { Text } from "react-native";

const MainScreen: FC = () => {
    return (
        <SafeAreaView>
            <Text>MainScreen of Statistic</Text>
        </SafeAreaView>
    )
}

export default MainScreen